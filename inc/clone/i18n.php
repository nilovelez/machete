<?php
/**
 * Localization file for the clone module.
 *
 * @package WordPress
 * @subpackage Machete
 */

if ( ! defined( 'MACHETE_ADMIN_INIT' ) ) {
	exit;
}

$this->params['title']       = __( 'Post & Page Cloner', 'machete' );
$this->params['full_title']  = __( 'Post & Page Cloner', 'machete' );
$this->params['description'] = __( 'Adds a "duplicate" link to post, page and most post types lists. Also adds "copy to new draft" function to the post editor.', 'machete' );
